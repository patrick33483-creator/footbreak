name: sync nginx2
on: workflow_dispatch
jobs:
  s:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null
      - env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
        run: |
          scp deploy/nginx-unified-dashboard.conf "$DEPLOY_USER@$DEPLOY_HOST:/tmp/nginx-unified-dashboard.conf"
          scp deploy/sync-nginx2.sh "$DEPLOY_USER@$DEPLOY_HOST:/tmp/sync-nginx2.sh"
          ssh "$DEPLOY_USER@$DEPLOY_HOST" "sudo chmod +x /tmp/sync-nginx2.sh && sudo /tmp/sync-nginx2.sh"
